% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce.R
\name{hegel_reproduce}
\alias{hegel_reproduce}
\title{Replay a stored counterexample}
\usage{
hegel_reproduce(blob, property, ...)
}
\arguments{
\item{blob}{A reproduction blob string, as shown in a failure report.}

\item{property}{The property, as passed to \code{\link[=hegel_test]{hegel_test()}}.}

\item{...}{Unused; must be empty. Present for forward compatibility.}
}
\value{
Called for its output and side effects; a reproduced failure or
a stale blob is signaled as an error condition rather than returned.
}
\description{
Re-runs one property case from a reproduction \code{blob}, as
printed by a failing \code{\link[=hegel_test]{hegel_test()}} report, driving the property once
with draw reporting enabled.

If the property fails again, the failure is re-raised as a \code{hegelr_failure}
condition (inheriting from \code{error}) carrying the draw report and the
original message. If the property passes, the blob no longer reproduces
and an error of class \code{hegelr_stale_blob} is signaled. The Rust
binding treats this as a hard failure too: the bug may have been fixed,
or the blob may be stale.
}
\examples{
\donttest{
if (hegel_library_available()) {
  my_sort <- function(x) {
    out <- sort(x)
    out[!duplicated(out)] # the bug: drops duplicates
  }
  property <- function(tc) {
    xs <- tc$draw(g_vectors(g_integers(-100, 100), max_size = 20))
    if (!identical(my_sort(xs), sort(xs))) {
      stop("my_sort() does not match base::sort()")
    }
  }
  blob <- tryCatch(
    hegel_test(property),
    hegelr_failure = function(e) {
      b <- e$blobs[[1L]]
      if (!is.na(b) && nzchar(b)) b else NULL
    },
    error = function(e) NULL
  )
  if (is.character(blob)) {
    tryCatch(
      hegel_reproduce(blob, property),
      hegelr_failure = function(e) cat("still failing\n"),
      hegelr_stale_blob = function(e) cat("fixed or stale\n")
    )
  }
}
}
}
