% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-primitive.R
\name{g_doubles}
\alias{g_doubles}
\title{Generate doubles}
\usage{
g_doubles(
  min = -Inf,
  max = Inf,
  allow_nan = NULL,
  allow_infinity = NULL,
  exclude_min = FALSE,
  exclude_max = FALSE,
  smallest_nonzero = 4.94065645841247e-324
)
}
\arguments{
\item{min}{Lower bound. Defaults to \code{-Inf}.}

\item{max}{Upper bound. Defaults to \code{Inf}.}

\item{allow_nan}{Whether NaN may be generated (\code{NULL} = family
conditional default).}

\item{allow_infinity}{Whether infinite values may be generated (\code{NULL}
= family conditional default).}

\item{exclude_min}{Exclude \code{min} itself from the range (finite \code{min}
only).}

\item{exclude_max}{Exclude \code{max} itself from the range (finite \code{max}
only).}

\item{smallest_nonzero}{Smallest nonzero magnitude the engine will
produce; a positive finite number (default \code{5e-324}).}
}
\value{
A generator descriptor (class \code{hegelr_generator}) drawing one
double per case.
}
\description{
Draws doubles in \verb{[min, max]}, backed by the engine's
\code{generate_float} primitive (64-bit width). \code{allow_nan} and
\code{allow_infinity} default to \code{NULL}, resolved per the family rule: NaN
only when the range is unbounded on both ends, infinities only when
at least one end is unbounded. (Go: "true when no bounds are set,
false otherwise"; TypeScript: NaN when \emph{neither} side is bounded,
infinity when \emph{either} side is open.) Pass \code{TRUE}/\code{FALSE} to decide
explicitly.
}
\examples{
gen <- g_doubles(0, 1, smallest_nonzero = 1e-10)
gen$label
}
