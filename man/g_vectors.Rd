% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-composite.R
\name{g_vectors}
\alias{g_vectors}
\title{Generate atomic vectors}
\usage{
g_vectors(gen, min_size = 0, max_size = Inf)
}
\arguments{
\item{gen}{Element generator (built by a \verb{g_*()} function).}

\item{min_size}{Minimum length (whole number \verb{>= 0}).}

\item{max_size}{Maximum length (whole number, or \code{Inf} for unbounded).
\code{Inf} is sent to the engine as the 2^53 "no bound" sentinel.}
}
\value{
A generator descriptor (class \code{hegelr_generator}).
}
\description{
Draws an atomic vector whose elements come from \code{gen}; the
engine chooses the length through a collection session
(\code{new_collection} / \code{collection_more}) inside a LIST span. Elements
are unified into an atomic vector of the element type: doubles,
logicals and characters via \code{c()}; raw elements via \code{as.raw()}. An
empty draw yields the element generator's typed empty prototype when
the type is knowable without a draw (\code{numeric(0)} for \code{\link[=g_integers]{g_integers()}},
\code{raw(0)} for \code{\link[=g_raw]{g_raw()}}, ...). Otherwise the result is \code{NULL} (e.g.
under \code{\link[=g_map]{g_map()}}, whose output type depends on the transform).
}
\examples{
g_vectors(g_integers(-100, 100), max_size = 20)
}
