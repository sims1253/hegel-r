% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/libhegel.R
\name{hegel_install}
\alias{hegel_install}
\title{Install the libhegel engine}
\usage{
hegel_install(version = "latest")
}
\arguments{
\item{version}{Version to install: \code{"latest"} (the default) or an exact
release version such as \code{"0.32.5"} (with or without a leading \code{v}).}
}
\value{
The path of the installed library, invisibly.
}
\description{
Downloads the prebuilt libhegel engine library (a single
file such as \code{libhegel-windows-amd64.dll}) from the Hegel releases on
GitHub
(\url{https://github.com/hegeldev/hegel-rust/releases}) into
\verb{tools::R_user_dir("hegelr", "cache")/libhegel/<version>/}. The file
is stored under its canonical name (\code{hegel.dll}, \code{libhegel.so} or
\code{libhegel.dylib}), where \code{\link[=hegel_library_path]{hegel_library_path()}} finds it
automatically. When the release publishes a \code{.sha256} sidecar for the
asset, the download is verified against it (requires R >= 4.5; skipped
with a message on older R).

This function needs the \emph{jsonlite} package (a soft dependency) to read
the GitHub API response, and network access. The GitHub API requires a
\code{User-Agent} header; hegel_install() sends it on every request.
}
\examples{
\donttest{
if (interactive() && !hegel_library_available()) {
  hegel_install()
  hegel_version()
}
}
}
