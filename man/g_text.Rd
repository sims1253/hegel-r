% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-text.R
\name{g_text}
\alias{g_text}
\title{Generate text strings}
\usage{
g_text(
  min_size = 0,
  max_size = Inf,
  codec = "utf-8",
  min_codepoint = 0,
  max_codepoint = 2^32 - 1,
  categories = NULL,
  exclude_categories = NULL,
  include_characters = NULL,
  exclude_characters = NULL,
  alphabet = NULL
)
}
\arguments{
\item{min_size}{Minimum string length (whole number \verb{>= 0}).}

\item{max_size}{Maximum string length (whole number \verb{>= 0}, or \code{Inf} for
unbounded - the default).}

\item{codec}{Encoding hint, \code{"utf-8"} by default, or \code{NULL} for the
engine default.}

\item{min_codepoint}{Smallest allowed codepoint (default 0).}

\item{max_codepoint}{Largest allowed codepoint (default \code{2^32 - 1}).}

\item{categories}{Optional character vector of allowed Unicode
categories.}

\item{exclude_categories}{Optional character vector of excluded Unicode
categories.}

\item{include_characters}{Optional string of always-allowed characters.}

\item{exclude_characters}{Optional string of excluded characters.}

\item{alphabet}{Optional string of allowed characters; sugar for
\code{include_characters}.}
}
\value{
A generator descriptor (class \code{hegelr_generator}) drawing one
character string per case.
}
\description{
Draws strings with a length in \verb{[min_size, max_size]}
(unbounded above by default, per the family's \code{text()}) from
codepoints in \verb{[min_codepoint, max_codepoint]}, optionally restricted
to (or excluding) Unicode categories and character sets. Backed by
the engine's text string generator.

\code{alphabet} is sugar for \code{include_characters}: a string whose
characters form the allowed set. Pass either \code{alphabet} or
\code{include_characters}, not both.
}
\examples{
g_text(max_size = 8)
g_text(alphabet = "abc")
}
