% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{hegel_test}
\alias{hegel_test}
\title{Run a property-based test}
\usage{
hegel_test(
  property,
  test_cases = 100,
  seed = NULL,
  derandomize = NULL,
  database = TRUE,
  verbosity = NULL,
  phases = NULL,
  single_test_case = FALSE,
  suppress_health_checks = NULL,
  report_multiple_failures = FALSE
)
}
\arguments{
\item{property}{A function taking a single argument (the test case).}

\item{test_cases}{Number of test cases to aim for (whole number
\verb{>= 1}). Defaults to 100.}

\item{seed}{Optional fixed seed (whole number, \verb{|seed| <= 2^53}),
making the run deterministic; negative values wrap like a two's
complement cast, matching the Go binding.}

\item{derandomize}{Use a fixed seed derived from the database key
instead of a random one. \code{NULL} (the default) defers to the engine,
which enables derandomization in CI environments; pass \code{TRUE}/\code{FALSE}
to decide explicitly.}

\item{database}{\code{TRUE} (default): persist failing examples under
\verb{tools::R_user_dir("hegelr", "cache")/examples/}, a stable location
(the engine's own default depends on the working directory); hegelr
disables the database automatically in CI, like the other Hegel
bindings. \code{FALSE}: disable the database. A directory path string: use
that directory.}

\item{verbosity}{Engine verbosity, \code{NULL} (default) for the engine
default, or a whole number in \verb{0..3}. At \code{verbosity >= 2} each test
case's draws and notes are shown as it runs; \code{verbosity = 0} (quiet)
reduces the failure report to the failing call site.}

\item{phases}{Which engine phases to run: \code{NULL} (default) for all, or a
character vector subset of \code{"explicit"}, \code{"reuse"}, \code{"generate"},
\code{"target"}, \code{"shrink"} (named per the official bindings' \code{Phase}
enums). E.g. \code{phases = c("reuse", "generate")} disables shrinking;
\code{phases = "generate"} runs only freshly generated examples.}

\item{single_test_case}{Run exactly one generated test case with no
shrinking, replay, or example database (Go binding's
\code{WithSingleTestCase()}, Rust's \code{Mode::SingleTestCase}). Useful for
long-running workloads or bodies not safely re-runnable on the same
inputs; draw and note output is shown for the case.}

\item{suppress_health_checks}{\code{NULL} (default) or a character vector of
health checks to disable: \code{"filter_too_much"}, \code{"too_slow"},
\code{"test_cases_too_large"}, \code{"large_initial_test_case"}.}

\item{report_multiple_failures}{Keep generating after the first failure
to surface additional distinct bugs (grouped by origin). Defaults to
\code{FALSE} (stop at the first failure).}
}
\value{
\code{TRUE}, invisibly, when the property holds. On failure or an
engine-level error, an error condition is signaled (class
\code{hegelr_failure} for falsified properties, \code{hegelr_run_error} for
engine errors).
}
\description{
\code{hegel_test()} drives the Hegel engine for \code{property}, a
plain R closure taking one argument: the test case object \code{tc}. Inside
the body:
\itemize{
\item \code{tc$draw(gen)} draws a value from a generator (\code{g_integers()},
\code{g_vectors()}, ...); all randomness must come from draws so the
engine can shrink and replay.
\item \code{tc$assume(cond)} rejects the current case when \code{cond} is not \code{TRUE}
(the case is retried, not failed).
\item \code{tc$note(msg)} records a message shown only in the failure report.
\item \code{tc$target(value, label = NULL)} reports a targeting score for the
engine's target phase, guiding generation toward larger scores.
\item \code{stop()}, \code{stopifnot()} and testthat expectations fail the case.
}

Each case's outcome maps to an engine status: a body that returns
normally is VALID, \code{hegelr_assume} conditions are INVALID, budget
exhaustion is OVERRUN (the engine signals \code{hegelr_stop_test} when the
draw budget runs out), and any other error is INTERESTING - a
counterexample to shrink and report. This includes testthat expectation
failures, which inherit from \code{error}.

When the run fails, \code{hegel_test()} replays each shrunk counterexample
once with draw reporting, then signals a condition of class
\code{hegelr_failure} (inheriting from \code{error}). The message lists each
failure's draws as numbered assignment lines, an optional reproduce
hint, and the property's own error message; multiple failures get a
count headline:

\if{html}{\out{<div class="sourceCode">}}\preformatted{draw_1 <- c(0, 0)
Reproduce with: hegel_reproduce("<blob>", property)
identical(my_sort(x), sort(x)) is not TRUE
}\if{html}{\out{</div>}}

The condition carries \code{blobs}, \code{draws} and \code{notes} fields for
programmatic use. \code{verbosity = 0} reduces the report to the failing
call sites.
}
\examples{
if (hegel_library_available()) {
  # Passing property: sorting is idempotent.
  hegel_test(function(tc) {
    xs <- tc$draw(g_vectors(g_integers(-100, 100), max_size = 20))
    stopifnot(identical(sort(xs), sort(sort(xs))))
  })

  # Failing property: the classic sort-with-dedup bug. Hegel shrinks it
  # to a minimal counterexample (two equal elements); the example
  # catches the failure and prints it.
  my_sort <- function(x) {
    out <- sort(x)
    out[!duplicated(out)] # silently drops duplicates: the bug
  }
  tryCatch(
    hegel_test(function(tc) {
      xs <- tc$draw(g_vectors(g_integers(-100, 100), max_size = 20))
      if (!identical(my_sort(xs), sort(xs))) {
        stop("my_sort() does not match base::sort()")
      }
    }),
    error = function(e) cat("caught:", conditionMessage(e), "\n")
  )
}
}
