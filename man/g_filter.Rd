% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-composite.R
\name{g_filter}
\alias{g_filter}
\title{Filter a generator's output}
\usage{
g_filter(gen, predicate)
}
\arguments{
\item{gen}{Inner generator (built by a \verb{g_*()} function).}

\item{predicate}{Function returning \code{TRUE}/\code{FALSE} for acceptable
values.}
}
\value{
A generator descriptor (class \code{hegelr_generator}).
}
\description{
Repeatedly draws from \code{gen} inside a FILTER span. When
\code{predicate} rejects a value the span is stopped with
\code{discard = TRUE}, reverting the engine's choices for that attempt, and
the draw is retried. After 3 rejected attempts (the family cap, per
the Rust and Go bindings), the generator signals \code{hegelr_assume},
rejecting the test case (the case counts as INVALID, exactly like
\code{tc$assume(FALSE)}); the engine's \code{filter_too_much} health check is
the real guard against over-filtered generators.
}
\examples{
g_filter(g_integers(0, 100), function(x) x \%\% 7 == 0)
}
