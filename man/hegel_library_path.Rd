% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/libhegel.R
\name{hegel_library_path}
\alias{hegel_library_path}
\title{Resolve the path of the libhegel engine library}
\usage{
hegel_library_path()
}
\value{
A character string: an absolute library file path, or a bare
library name if no file was found (in which case loading may still
succeed via the OS loader).
}
\description{
\code{hegel_library_path()} locates the native libhegel engine
without loading it. Candidates are tried in this order:
\enumerate{
\item The \code{HEGEL_LIBHEGEL_PATH} environment variable, when it points at an
existing file.
\item Installs managed by \code{\link[=hegel_install]{hegel_install()}}: the highest-versioned
\verb{libhegel/<version>/} directory under \code{tools::R_user_dir("hegelr", "cache")}. An \code{installed.rds} marker (written by \code{hegel_install()})
is honored first; otherwise the directory is searched recursively for
\code{libhegel.so}, \code{libhegel.dylib} or \code{hegel.dll}.
\item Standard system locations on Unix-alikes: \verb{/usr/local/lib},
\verb{/usr/lib}, \verb{/opt/homebrew/lib}, \verb{/usr/local/homebrew/lib}.
\item A bare library name, letting the OS loader search \code{PATH} and the
system directories.
}

Only positive resolutions (steps 1-3) are cached for the process; the
bare-name fallback is returned uncached, so resolution still finds a
later install or environment-variable change.
}
\examples{
cat("libhegel resolves to:", hegel_library_path(), "\n")
}
