% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-text.R
\name{g_characters}
\alias{g_characters}
\title{Generate single characters}
\usage{
g_characters(
  codec = "utf-8",
  min_codepoint = 0,
  max_codepoint = 2^32 - 1,
  categories = NULL,
  exclude_categories = NULL,
  include_characters = NULL,
  exclude_characters = NULL,
  alphabet = NULL
)
}
\arguments{
\item{codec}{Encoding hint, \code{"utf-8"} by default, or \code{NULL} for the
engine default.}

\item{min_codepoint}{Smallest allowed codepoint (default 0).}

\item{max_codepoint}{Largest allowed codepoint (default \code{2^32 - 1}).}

\item{categories}{Optional character vector of allowed Unicode
categories.}

\item{exclude_categories}{Optional character vector of excluded Unicode
categories.}

\item{include_characters}{Optional string of always-allowed characters.}

\item{exclude_characters}{Optional string of excluded characters.}

\item{alphabet}{Optional string of allowed characters; sugar for
\code{include_characters}.}
}
\value{
A generator descriptor (class \code{hegelr_generator}) drawing one
single-character string per case.
}
\description{
Draws a single character (one Unicode codepoint) per case -
the family's \code{characters()}: a text generator fixed to length 1.
Constraint parameters are identical to \code{\link[=g_text]{g_text()}}.
}
\examples{
g_characters(categories = c("Lu", "Ll"))
}
