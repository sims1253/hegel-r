% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators-primitive.R
\name{g_integers}
\alias{g_integers}
\title{Generate integers}
\usage{
g_integers(min = -2^53, max = 2^53)
}
\arguments{
\item{min}{Lower bound (whole number, \verb{>= -2^53}). Defaults to \code{-2^53}.}

\item{max}{Upper bound (whole number, \verb{<= 2^53}). Defaults to \code{2^53}.}
}
\value{
A generator descriptor (class \code{hegelr_generator}) drawing one
numeric value per \code{\link[=hegel_test]{hegel_test()}} case.
}
\description{
Draws integers in \verb{[min, max]} (both inclusive). Values are
returned as R \strong{doubles}: the ABI carries integers as doubles
restricted to the u64-safe subset (whole numbers with \verb{|x| <= 2^53}).
\code{g_integers()} cannot produce or accept anything outside that range.
}
\examples{
# Building a generator needs no engine; gen$label previews the draw
# report.
gen <- g_integers(1, 10)
gen$label
}
